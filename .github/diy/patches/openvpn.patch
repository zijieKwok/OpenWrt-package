--- a/openvpn/files/openvpn.init
+++ b/openvpn/files/openvpn.init
@@ -144,6 +144,7 @@ openvpn_add_instance() {
 	local security="$4"
 	local up="$5"
 	local down="$6"
+	local client="$7"
 
 	procd_open_instance "$name"
 	procd_set_param command "$PROG"	\
@@ -155,7 +156,7 @@ openvpn_add_instance() {
 		--down "/usr/libexec/openvpn-hotplug down $name" \
 		--route-up "/usr/libexec/openvpn-hotplug route-up $name" \
 		--route-pre-down "/usr/libexec/openvpn-hotplug route-pre-down $name" \
-		--ipchange "/usr/libexec/openvpn-hotplug ipchange $name" \
+		${client:+--ipchange "/usr/libexec/openvpn-hotplug ipchange $name"} \
 		${up:+--setenv user_up "$up"} \
 		${down:+--setenv user_down "$down"} \
 		--script-security "${security:-2}" \
@@ -181,10 +182,13 @@ start_instance() {
 		return 1
 	}
 
-	local up down script_security
+	local up down script_security client
 	config_get up "$s" up
 	config_get down "$s" down
 	config_get script_security "$s" script_security
+	config_get client "$s" client
+
+	[ -n "$client" ] && [ "$client" -eq 0 ] && unset client
 
 	[ ! -d "/var/run" ] && mkdir -p "/var/run"
 
@@ -192,7 +196,9 @@ start_instance() {
 		append UCI_STARTED "$config" "$LIST_SEP"
 		[ -n "$up" ] || get_openvpn_option "$config" up up
 		[ -n "$down" ] || get_openvpn_option "$config" down down
-		openvpn_add_instance "$s" "${config%/*}" "$config" "$script_security" "$up" "$down"
+		[ -n "$client" ] || get_openvpn_option "$config" client client
+		[ "$client" -eq 0 ] && unset client
+		openvpn_add_instance "$s" "${config%/*}" "$config" "$script_security" "$up" "$down" "$client"
 		return
 	fi
 
@@ -202,7 +208,7 @@ start_instance() {
 	append_params "$s" $OPENVPN_PARAMS
 	append_list "$s" $OPENVPN_LIST
 
-	openvpn_add_instance "$s" "/var/etc" "openvpn-$s.conf" "$script_security" "$up" "$down"
+	openvpn_add_instance "$s" "/var/etc" "openvpn-$s.conf" "$script_security" "$up" "$down" "$client"
 }
 
 start_service() {
@@ -246,7 +252,8 @@ start_service() {
 
 				get_openvpn_option "$path" up up || up=""
 				get_openvpn_option "$path" down down || down=""
-				openvpn_add_instance "$name" "${path%/*}" "$path" "" "$up" "$down"
+				get_openvpn_option "$path" client client || client=""
+				openvpn_add_instance "$name" "${path%/*}" "$path" "" "$up" "$down" "$client"
 			fi
 		done
 	fi
